# Copy this file to credentials.env and fill in your Dakota portal login.
#   copy credentials.env.example credentials.env
#
# credentials.env is gitignored — never commit real passwords.
# Jenkins uses its own credential store (dakota-portal-creds); this file is for local pytest runs.

DAKOTA_USERNAME=your.email@dakota.com
DAKOTA_PASSWORD=your-password
